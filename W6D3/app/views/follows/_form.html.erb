<!-- <% if current_user.follows?(user) %>
  <%= button_to "Unfollow", user_follow_url(user), method: :delete %>
<% else %>
  <%= button_to "Follow", user_follow_url(user), method: :post %>
<% end %> -->

<% follow = current_user.follows?(user) ? "followed" : "unfollowed" %>

<button class="follow-toggle" data-user-id="<%= user.id %>" data-initial-follow-state="<%= follow %>" type="button" name="button"></button>

<br>
<br>
<br>
